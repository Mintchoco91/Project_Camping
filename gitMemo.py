1. 변경내용 초기화
git reset --hard

2. 원격 브랜치 목록 동기화
git remote update --prune

3. 목록 확인
git branch -a  

4. checkout 으로 최신브랜치 가져옴
git checkout [브랜치명]

5. IDE 에서 로컬브랜치 만들어서 기존처럼 작업

6. 브랜치 삭제
git branch -D [브랜치명]